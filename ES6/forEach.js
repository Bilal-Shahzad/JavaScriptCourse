// forEach()
// calls a provided callback function once for each element in an array in ascending order


// syntax

// array.forEach(function(currentValue, index, array) thisValue)
// in order 

// array is the array 
// for.Each is the keyword 
// function is the call back function 
// currentValue is the current value iterating through 
// index (optional)
// array object (optional)
// thisValue (optional)

// forLoop 
var scaryCreatures = ['lions', 'tigers', 'bears']
for( var i = 0; i < scaryCreatures.length; i++) {
    console.log(`Dorothy is afraid of ${scaryCreaturs[i]}.`))
}

// forEach
scaryCreatures.forEach(function(scaryCreatures) {
    console.log(`Dorothy is afraid of ${scaryCreatures})`)
    
})

